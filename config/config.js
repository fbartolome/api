const host = 'localhost';
const port = 3000;
const dbuser = 'root';
const dbpass = 'password';
const database = 'clappsdb';

module.exports = {
    host: host,
    port: port,
    dbuser: dbuser,
    dbpass: dbpass,
    database: database,
}